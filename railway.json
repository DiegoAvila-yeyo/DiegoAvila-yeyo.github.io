{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && npm install && npx prisma generate && node src/server.js",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/api/health"
  }
}